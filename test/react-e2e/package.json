{
  "private": true,
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "start:app": "pnpm -w run nx serve react-react-app",
    "test:e2e": "start-server-and-test start:app http-get://127.0.0.1:3000 cypress:run"
  }
}
